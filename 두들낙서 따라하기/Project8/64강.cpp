#include <iostream>
/*
using namespace std;

class myclass
{
public: myclass() :num(cnt++), ch('\0'){}

		void check(myclass* ptr)
		{
			if (ptr + num == this)
			{
				cout << num << endl;
			}
		}
		static int cnt;
private:
	int num;
	char ch;
};

int myclass::cnt = 0;

int main()
{
	myclass obj[5];

	cout << "test #1 :" << endl;
	for (int i = 0; i < 5; i++)
	{
		obj[i].check(obj);
	}
	cout << "test #2 :" << endl;
	for (myclass &i : obj)
	{
		i.check(obj);
	}
	cout << "cnt =" << myclass::cnt << endl;
}
*/